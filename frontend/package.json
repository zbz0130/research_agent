{
  "name": "wishforge-frontend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 1420",
    "test": "node --test src/**/*.test.js",
    "check": "node --check app.js && node --check smoke-browser.mjs && node --check src/main.js && node --check src/graph/graph-renderer.js && node --check src/graph/graph-layout.js && node --check src/graph/graph-inspector.js && node --check src/graph/graph-metrics.js",
    "smoke:browser": "node smoke-browser.mjs"
  },
  "dependencies": {
    "cytoscape": "^3.33.1"
  },
  "devDependencies": {
    "puppeteer-core": "^24.16.0",
    "vite": "^7.1.1"
  }
}
